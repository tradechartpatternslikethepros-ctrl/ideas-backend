# Copy to .env (for local dev). On Railway, set these in Variables UI.
PORT=8080
API_TOKEN=supersecret123
ALLOWED_ORIGIN=*
BASE_URL=https://amused-prosperity-production.up.railway.app
DATA_DIR=./data
